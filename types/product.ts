export interface Product {
  id: number;
  name: string;
  brand: string;
  price: string;
  image: string;
  description: string;

  screen: string;
  ram: string;
  storage: string;
  camera: string;
  battery: string;

  featured: boolean;
}